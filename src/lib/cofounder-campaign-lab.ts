import { getCofounderSetting, isoNow, setCofounderSetting } from "@/lib/cofounder-store";
import { writeAdminLog } from "@/lib/mcp-shared";
export const DEFAULT_CAMPAIGN_LAB = { campaignId: "traction-lab-3000", name: "Vuka Traction Lab", budgetZAR: 3000, status: "planning", tracks: { artists: { label: "Artist acquisition + activation", allocationZAR: 1800 }, friends: { label: "Friends/fans + transaction activation", allocationZAR: 1200 } }, reserveZAR: 0, events: [], commitments: [], createdAt: isoNow() };
export async function initializeCampaignLab(budgetZAR:number, artistsAllocationZAR:number, friendsAllocationZAR:number, reserveZAR:number) { const total=artistsAllocationZAR+friendsAllocationZAR+reserveZAR; if(budgetZAR<0||budgetZAR>3000||total>budgetZAR) throw new Error(`Budget exceeded: R${total.toFixed(2)} against R${budgetZAR.toFixed(2)}.`); const existing=await getCofounderSetting<any>("cofounder.campaign.lab",null); if(existing?.events?.length||existing?.commitments?.length) throw new Error("Campaign lab already has activity; refusing to reset it."); const lab={...DEFAULT_CAMPAIGN_LAB,budgetZAR,tracks:{artists:{...DEFAULT_CAMPAIGN_LAB.tracks.artists,allocationZAR:artistsAllocationZAR},friends:{...DEFAULT_CAMPAIGN_LAB.tracks.friends,allocationZAR:friendsAllocationZAR}},reserveZAR,createdAt:isoNow(),updatedAt:isoNow()}; await setCofounderSetting("cofounder.campaign.lab",lab); await writeAdminLog("cofounder.campaign_lab_initialized","Campaign",lab.campaignId,JSON.stringify(lab)); return lab; }
export async function getCampaignLabStatus(){const lab=await getCofounderSetting<any>("cofounder.campaign.lab",DEFAULT_CAMPAIGN_LAB),cs=lab.commitments??[],es=lab.events??[],committedZAR=cs.reduce((s:number,c:any)=>s+(c.amountZAR??0),0),allocatedZAR=(lab.tracks?.artists?.allocationZAR??0)+(lab.tracks?.friends?.allocationZAR??0)+(lab.reserveZAR??0);const byTrack:any={};for(const track of ["artists","friends"]){const te=es.filter((e:any)=>e.track===track),tc=cs.filter((c:any)=>c.track===track);byTrack[track]={allocationZAR:lab.tracks?.[track]?.allocationZAR??0,committedZAR:tc.reduce((s:number,c:any)=>s+(c.amountZAR??0),0),events:te.length,transactions:te.filter((e:any)=>e.type==="genuine_transaction").length,transactionValueZAR:te.filter((e:any)=>e.type==="genuine_transaction").reduce((s:number,e:any)=>s+(e.valueZAR??0),0)}}return{lab,byTrack,allocatedZAR,committedZAR,uncommittedZAR:lab.budgetZAR-committedZAR,unallocatedZAR:lab.budgetZAR-allocatedZAR,budgetSafe:allocatedZAR<=lab.budgetZAR&&committedZAR<=lab.budgetZAR,generatedAt:isoNow()};}
export async function allocateCampaignLabBudget(a:number,f:number,r:number){const lab=await getCofounderSetting<any>("cofounder.campaign.lab",DEFAULT_CAMPAIGN_LAB),allocation=a+f+r,committed=(lab.commitments??[]).reduce((s:number,c:any)=>s+(c.amountZAR??0),0);if(allocation>lab.budgetZAR||allocation<committed)throw new Error(`Allocation blocked: R${allocation.toFixed(2)} requested, R${committed.toFixed(2)} committed, R${lab.budgetZAR.toFixed(2)} ceiling.`);const next={...lab,tracks:{...lab.tracks,artists:{...lab.tracks.artists,allocationZAR:a},friends:{...lab.tracks.friends,allocationZAR:f}},reserveZAR:r,updatedAt:isoNow()};await setCofounderSetting("cofounder.campaign.lab",next);await writeAdminLog("cofounder.campaign_lab_reallocated","Campaign",lab.campaignId,JSON.stringify({a,f,r}));return next;}
export async function commitCampaignLabReward(track:"artists"|"friends",recipient:string,amountZAR:number,reason:string,evidence:string){const lab=await getCofounderSetting<any>("cofounder.campaign.lab",DEFAULT_CAMPAIGN_LAB),cs=lab.commitments??[],committed=cs.reduce((s:number,c:any)=>s+(c.amountZAR??0),0),tc=cs.filter((c:any)=>c.track===track).reduce((s:number,c:any)=>s+(c.amountZAR??0),0),allocation=lab.tracks?.[track]?.allocationZAR??0;if(committed+amountZAR>lab.budgetZAR||tc+amountZAR>allocation)throw new Error("Campaign budget guard blocked this commitment.");const c={id:crypto.randomUUID(),track,recipient,amountZAR,reason,evidence,status:"reserved_pending_approval",createdAt:isoNow()};await setCofounderSetting("cofounder.campaign.lab",{...lab,commitments:[c,...cs],updatedAt:isoNow()});await writeAdminLog("cofounder.campaign_lab_commitment","Campaign",lab.campaignId,JSON.stringify(c));return c;}
export async function recordCampaignLabEvent(input:any){if(input.type==="genuine_transaction"&&(!input.valueZAR||input.valueZAR<=0))throw new Error("A genuine_transaction event requires a positive valueZAR.");const lab=await getCofounderSetting<any>("cofounder.campaign.lab",DEFAULT_CAMPAIGN_LAB),event={id:crypto.randomUUID(),...input,recordedAt:isoNow()};await setCofounderSetting("cofounder.campaign.lab",{...lab,events:[event,...(lab.events??[])],updatedAt:isoNow()});await writeAdminLog("cofounder.campaign_lab_event","Campaign",lab.campaignId,JSON.stringify(event));return event;}
